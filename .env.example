# Sign-Speech Bridge — Next.js frontend (Phase 2)
# Copy to .env.local and fill in. Use same Stream keys as agent/.

# Stream — public key for client; secret only for server (token API)
NEXT_PUBLIC_STREAM_API_KEY=
STREAM_API_SECRET=

# Agent server — base URL of the Vision Agents HTTP server
NEXT_PUBLIC_AGENT_SERVER_URL=http://localhost:8000
